const INPUTS = Object.freeze({
  WORKDIR: 'workdir',
  GPG_PRIVATE_KEY: 'gpg-private-key',
  GPG_PASSPHRASE: 'gpg-passphrase',
  GPG_FINGERPRINT: 'gpg-fingerprint',
  GPG_TRUST_LEVEL: 'gpg-trust-level',
  GIT_SCOPE: 'git-scope',
  GIT_SIGN_USER: 'git-sign-user',
  GIT_SIGN_COMMIT: 'git-sign-commit',
  GIT_SIGN_TAG: 'git-sign-tag',
  GIT_SIGN_PUSH: 'git-sign-push',
  TOKEN: 'token'
})

export default INPUTS
